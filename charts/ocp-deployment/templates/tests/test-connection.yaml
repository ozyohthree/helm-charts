apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ocp-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "ocp-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ocp-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
